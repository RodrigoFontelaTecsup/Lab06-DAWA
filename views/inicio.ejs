<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pagina de inicio</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <style>
      html,
      body {
        height: 100%;
        margin: 0;
      }

      body {
        display: flex;
        flex-direction: column;
      }

      .container {
        flex: 1;
      }

      .image-and-text {
        display: flex;
        flex-direction: row;
        align-items: center;
      }

      .img-fluid {
        max-width: 100%;
        margin-right: 20px;
      }
    </style>
  </head>
  <body>
    <%- include('navbar.ejs') %>
    <div class="container">
      <div class="row">
        <div class="image-and-text">
          <img
            class="img-fluid"
            src="<%= imagen_inicio %>"
            alt="Imagen de la empresa"
          />
          <div>
            <h1 class="mt-4 mb-4">Como inicio y evoluciono nuestra empresa</h1>
            <p><%= inicio %></p>
            <p><%= evolucion %></p>
            <p><%= inicio_valores %></p>
            <ul class="list-values">
              <li><%= valor_innovacion %></li>
              <li><%= valor_calidad %></li>
              <li><%= valor_colaboracion %></li>
              <li><%= valor_responsabilidad %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    <%- include('footer.ejs') %>
  </body>
</html>
